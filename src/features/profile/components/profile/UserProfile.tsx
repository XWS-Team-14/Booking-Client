import classNames from 'classnames';

import styles from './UserProfile.module.scss';

const UserProfile = () => {
  return (
    <div className={classNames('frostedGlass', styles.profileWrapper)}>Hi</div>
  );
};

export default UserProfile;
